<?xml version="1.0" encoding="UTF-8" ?>

<templates xml:space="preserve">

	<!-- Add t-ref="nav" to the nav element for _updateMenuAppsIcon -->
	<t
		t-name="t4_theme.NavBar.Nav"
		t-inherit="web.NavBar"
		t-inherit-mode="extension"
	>
		<xpath expr="//nav" position="attributes">
			<attribute name="t-ref">nav</attribute>
		</xpath>
	</t>

	<t
		t-name="t4_theme.NavBar.AppsMenu"
		t-inherit="web.NavBar.AppsMenu"
		t-inherit-mode="extension"
	>
		<xpath expr="//Dropdown" position="replace">
			<!-- Overlay mode: Enterprise-style fullscreen HomeMenu via action -->
			<t t-if="this.hm">
				<a href="/odoo" class="o_menu_toggle border-0"
					t-att-class="{'hasImage': currentApp?.webIconData}"
					accesskey="h" t-ref="menuApps"
					t-on-click.prevent="() => { env.isSmall ? this._openAppMenuSidebar() : this.hm.toggle() }">
					<t t-if="env.isSmall and !this.hm.hasHomeMenu">
						<t t-call="web.NavBar.AppsMenu.Sidebar"/>
					</t>
					<t t-else="">
						<svg class="o_menu_toggle_icon pe-none" width="14px" height="14px" viewBox="0 0 14 14" xmlns="http://www.w3.org/2000/svg">
							<g t-foreach="[0, 5, 10]" t-as="Y" t-att-id="'o_menu_toggle_row_' + Y_index" fill="currentColor" t-key="'o_menu_toggle_row_' + Y_index">
								<rect t-foreach="[0, 5, 10]" t-as="X" width="3" height="3" t-att-x="X" t-att-y="Y" t-key="'o_menu_toggle_cell_' + X_index"/>
							</g>
						</svg>
						<t t-if="!env.isSmall and currentApp">
							<img
								t-if="currentApp.webIconData"
								t-att-src="currentApp.webIconData"
								class="o_menu_brand_icon d-inline position-absolute start-0 h-100 ps-1 ms-2"
								t-att-alt="currentApp.name"
								t-ref="appIcon"/>
							<span t-esc="currentApp.name" class="o_menu_brand d-flex ms-3 pe-0"/>
						</t>
					</t>
				</a>
			</t>
			<!-- Standard mode: Odoo default dropdown (text list) -->
			<t t-else="">
				<Dropdown>
					<button data-hotkey="h" title="Home Menu">
						<i class="oi oi-apps" />
					</button>
					<t t-set-slot="content">
						<DropdownItem
							t-foreach="apps"
							t-as="app"
							t-key="app.id"
							class="{ 'o_app': true, focus: menuService.getCurrentApp() === app }"
							onSelected="() => this.onNavBarDropdownItemSelection(app)"
							t-esc="app.name"
							attrs="{ href: getMenuItemHref(app), 'data-menu-xmlid': app.xmlid, 'data-section': app.id }"
						/>
					</t>
				</Dropdown>
			</t>
		</xpath>
	</t>

	<!-- Hide brand DropdownItem when in overlay mode (enterprise handles it in the toggle button) -->
	<t
		t-name="t4_theme.NavBar.Brand"
		t-inherit="web.NavBar"
		t-inherit-mode="extension"
	>
		<xpath expr="//DropdownItem[@t-esc='currentApp.name']" position="attributes">
			<attribute name="t-if">!this.hm and !env.isSmall and currentApp</attribute>
		</xpath>
	</t>

</templates>
